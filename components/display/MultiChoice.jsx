import { View } from "react-native";
import { CustomInput } from "../common/Input";
import CustomText from "../common/Text";

export const MultipleChoice = ({ answers }) => {
  return <View></View>;
};
